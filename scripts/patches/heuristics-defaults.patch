--- a/tests/conftest.py
+++ b/tests/conftest.py
@@ -53,22 +53,28 @@ HEURISTICS_SCHEMA = """
 CREATE TABLE IF NOT EXISTS heuristics (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     domain TEXT NOT NULL,
     rule TEXT NOT NULL,
     explanation TEXT,
     source_type TEXT,
     source_id INTEGER,
     confidence REAL DEFAULT 0.5,
     times_validated INTEGER DEFAULT 0,
     times_violated INTEGER DEFAULT 0,
     times_contradicted INTEGER DEFAULT 0,
     is_golden INTEGER DEFAULT 0,
-    status TEXT DEFAULT 'active',
+    status TEXT NOT NULL DEFAULT 'active',
     dormant_since DATETIME,
-    last_used_at DATETIME,
+    last_used_at DATETIME DEFAULT CURRENT_TIMESTAMP,
     revival_conditions TEXT,
     times_revived INTEGER DEFAULT 0,
-    min_applications INTEGER DEFAULT 10,
-    last_confidence_update DATETIME,
+    min_applications INTEGER NOT NULL DEFAULT 10,
+    last_confidence_update DATETIME DEFAULT CURRENT_TIMESTAMP,
     update_count_today INTEGER DEFAULT 0,
-    update_count_reset_date DATE,
-    confidence_ema REAL,
-    ema_alpha REAL,
+    update_count_reset_date DATE NOT NULL DEFAULT CURRENT_DATE,
+    confidence_ema REAL NOT NULL DEFAULT 0.5,
+    ema_alpha REAL NOT NULL DEFAULT 0.2,
     ema_warmup_remaining INTEGER DEFAULT 0,
-    last_ema_update DATETIME,
+    last_ema_update DATETIME DEFAULT CURRENT_TIMESTAMP,
+    fraud_flags INTEGER DEFAULT 0,
+    is_quarantined INTEGER DEFAULT 0,
+    last_fraud_check DATETIME DEFAULT CURRENT_TIMESTAMP,
     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
     updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
